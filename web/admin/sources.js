const esc=(s='')=>String(s).replace(/[&<>'\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','\"':'&quot;'}[c]));
fetch('../data/source_health.json',{cache:'no-store'}).then(r=>r.json()).then(d=>{document.getElementById('meta').textContent=d.generated_at?`Actualizado: ${new Date(d.generated_at).toLocaleString('es-CL')}`:'';const xs=Object.entries(d.sources||{}).sort((a,b)=>a[1].name.localeCompare(b[1].name));document.getElementById('cards').innerHTML=xs.map(([k,s])=>`<div class="card"><div><div class="name">${esc(s.name||k)}</div><div class="${['ok','error'].includes(s.status)?s.status:'warning'}">${s.status==='ok'?'✓ Operativa':s.status==='error'?'✕ Error':'⚠ Revisar'}</div>${s.error?`<div class="small">${esc(s.error)}</div>`:''}</div><div><b>${s.discovered??0}</b><div class="small">detectados</div></div><div><b>${s.new??0}</b><div class="small">nuevos</div></div><div><b>${s.published??0}</b><div class="small">publicados</div></div><div><b>${s.pending??0}</b><div class="small">pendientes</div></div><div><b>${s.rejected??0}</b><div class="small">descartados</div></div><div><b>${esc(s.last_signal_at||'—')}</b><div class="small">última señal</div></div></div>`).join('')}).catch(()=>document.getElementById('cards').innerHTML='<p>No se pudo cargar source_health.json.</p>');
