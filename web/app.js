const esc=(s='')=>String(s).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
let allSignals=[],activeType='Todos',activeScope='Todos',sortMode='personalized';
let previousVisit=localStorage.getItem('alicanto_last_visit');
let periodMode=previousVisit?'last':'14';
const readSet=new Set(JSON.parse(localStorage.getItem('alicanto_read')||localStorage.getItem('radar_read')||'[]'));
const interestState=JSON.parse(localStorage.getItem('alicanto_interests')||'{"types":[],"scopes":[]}');
function idFor(s){return btoa(unescape(encodeURIComponent((s.source_url||'')+'|'+(s.title||'')))).slice(0,120)}
function isRead(s){return readSet.has(idFor(s))}
function markRead(s){const id=idFor(s);if(readSet.has(id))readSet.delete(id);else readSet.add(id);localStorage.setItem('alicanto_read',JSON.stringify([...readSet]));draw()}
function arr(x){return Array.isArray(x)?x:[]}
function typeTags(s){return arr(s.signal_types)} function scopeTags(s){return arr(s.scopes)}
function rel(score){const n=Number(score||0);if(n>=85)return{label:'Esencial',cls:'essential'};if(n>=65)return{label:'Relevante',cls:'relevant'};return{label:'Contexto',cls:'context'}}
function fmtDate(raw){if(!raw)return'';const d=new Date(raw);if(Number.isNaN(d.getTime()))return esc(raw);return new Intl.DateTimeFormat('es-CL',{day:'2-digit',month:'short',year:'numeric',timeZone:'America/Santiago'}).format(d)}
function fmtUpdated(raw){const d=new Date(raw);if(Number.isNaN(d.getTime()))return'';const t=new Intl.DateTimeFormat('es-CL',{hour:'2-digit',minute:'2-digit',hour12:false,timeZone:'America/Santiago'}).format(d);return`Actualizado ${fmtDate(raw)}, ${t}`}
function tagHtml(s){return [...typeTags(s).map(x=>`<span class="tag">${esc(x)}</span>`),...scopeTags(s).slice(0,3).map(x=>`<span class="tag scope">${esc(x)}</span>`)].join('')}
function listBlock(title,xs){return xs.length?`<div class="intel"><div class="intel-title">${title}</div><ul>${xs.map(x=>`<li>${esc(x)}</li>`).join('')}</ul></div>`:''}
function contextBlock(xs){if(!xs.length)return'';return`<details class="related"><summary>Contexto relacionado · ${xs.length}</summary>${xs.map(x=>`<div class="related-item"><div class="related-title">${esc(x.title||'Norma relacionada')}</div>${x.relationship?`<div class="relationship">${esc(x.relationship)}</div>`:''}${x.summary?`<div class="related-desc">${esc(x.summary)}</div>`:''}${x.event_date?`<div class="related-desc">Fecha: ${fmtDate(x.event_date)}</div>`:''}${x.url?`<a href="${esc(x.url)}" target="_blank" rel="noopener">Ver antecedente ↗</a>`:''}</div>`).join('')}</details>`}
function sourceBlock(xs){if(!xs.length)return'';return`<details class="related sourceverify"><summary>Otras fuentes que verifican este hallazgo · ${xs.length}</summary>${xs.map(x=>`<div class="related-item"><div class="related-title">${esc(x.source_name||'Fuente')}</div><a href="${esc(x.url)}" target="_blank" rel="noopener">${esc(x.title||'Ver publicación')} ↗</a></div>`).join('')}</details>`}
function card(s){const r=rel(s.radar_score),rd=isRead(s);return`<article class="signal ${rd?'read':''}"><div class="cardhead"><div class="tagrow"><span class="priority ${r.cls}">${r.label}</span>${tagHtml(s)}</div><button class="readbtn ${rd?'done':''}" data-read="${esc(idFor(s))}">${rd?'↺ Marcar no leído':'✓ Marcar como leído'}</button></div><div class="body"><h2>${esc(s.title)}</h2>${s.what_happened?`<p><span class="label">Qué pasó:</span> ${esc(s.what_happened)}</p>`:''}${s.why_it_matters?`<p><span class="label">Por qué importa:</span> ${esc(s.why_it_matters)}</p>`:''}${typeTags(s).includes('Normativa')?`<p><span class="label">Vigencia:</span> ${esc(s.validity_text||'No identificada automáticamente')}</p>`:''}${listBlock('Puntos clave',arr(s.key_points).slice(0,3))}${listBlock('Aspectos a revisar',arr(s.risk_notes).slice(0,3))}<div class="source">${s.event_date?`Fecha de publicación: ${fmtDate(s.event_date)} · `:''}Fuente: ${esc(s.source_name||'')}${s.source_url?` · <a href="${esc(s.source_url)}" target="_blank" rel="noopener">Ver fuente original ↗</a>`:''}</div>${contextBlock(arr(s.related_context))}${sourceBlock(arr(s.source_alternatives))}</div></article>`}
function dateVal(s){return new Date(s.event_date||0).getTime()||0}
function ageDays(s){return Math.max(0,(Date.now()-dateVal(s))/86400000)}
function freshness(s){const a=ageDays(s);if(a<=2)return 100;if(a<=7)return 88;if(a<=14)return 72;if(a<=30)return 48;if(a<=60)return 28;return 12}
function interestScore(s){let score=0;for(const x of typeTags(s))if(interestState.types.includes(x))score+=18;for(const x of scopeTags(s))if(interestState.scopes.includes(x))score+=22;return Math.min(45,score)}
function personalizedScore(s){const radar=Number(s.radar_score||0),action=Number(s.actionability_score||radar),scope=Number(s.scope_score||radar);const impact=Math.max(Number(s.regulatory_impact_score||0),Number(s.economic_impact_score||0),Number(s.editorial_relevance||0));return .45*radar+.13*action+.09*scope+.10*impact+.08*freshness(s)+.15*interestScore(s)}
function strategicTop(pool,n=4){let candidates=pool.filter(s=>!isRead(s)).map(s=>({s,base:personalizedScore(s)})),picked=[];while(candidates.length&&picked.length<n){let best=null,bestScore=-Infinity;for(const c of candidates){const sameSource=picked.filter(x=>x.source_name===c.s.source_name).length;const sameType=picked.some(x=>typeTags(x)[0]&&typeTags(x)[0]===typeTags(c.s)[0]);const adjusted=c.base-(sameSource*8)-(sameType?3:0);if(adjusted>bestScore){bestScore=adjusted;best=c}}picked.push(best.s);candidates=candidates.filter(x=>x!==best)}return picked}
function periodLabel(){if(periodMode==='last'&&previousVisit)return'Desde tu última visita';return`${periodMode} días`}
function inPeriod(s){if(!s.event_date)return false;if(periodMode==='last'&&previousVisit)return dateVal(s)>new Date(previousVisit).getTime();return ageDays(s)<=Number(periodMode)+1}
function visible(){let out=allSignals.filter(inPeriod).filter(s=>(activeType==='Todos'||typeTags(s).includes(activeType))&&(activeScope==='Todos'||scopeTags(s).includes(activeScope)));if(sortMode==='date')out.sort((a,b)=>(isRead(a)-isRead(b))||dateVal(b)-dateVal(a));else out.sort((a,b)=>(isRead(a)-isRead(b))||personalizedScore(b)-personalizedScore(a)||dateVal(b)-dateVal(a));return out}
function brief(v){const top=strategicTop(v,4),unread=v.filter(s=>!isRead(s)).length;document.getElementById('brief').innerHTML=`<section class="brief"><h2>Lo que debes saber en 30 segundos y aún no has leído</h2><p><strong>${unread}</strong> hallazgos no leídos · ${esc(periodLabel())}</p>${top.length?top.map((s,i)=>`<p><span class="briefnum">${i+1}</span> ${esc(s.title)}</p>`).join(''):'<p class="muted">Estás al día.</p>'}<p class="muted">Marca una señal como leída para que deje de ocupar tu atención y pase al final.</p></section>`}
function draw(){const v=visible();brief(v);document.getElementById('count').textContent=`${v.length} hallazgos`;document.getElementById('local').innerHTML=v.map(card).join('');document.querySelectorAll('[data-read]').forEach(b=>b.onclick=()=>{const s=v.find(x=>idFor(x)===b.dataset.read);if(s)markRead(s)})}
function chips(id,values,current,setter){document.getElementById(id).innerHTML=values.map(v=>`<button class="chip ${v===current?'active':''}" data-v="${esc(v)}">${esc(v)}</button>`).join('');document.querySelectorAll(`#${id} .chip`).forEach(b=>b.onclick=()=>{setter(b.dataset.v);renderFilters();draw()})}
function renderFilters(){const types=['Todos',...['Normativa','Legal','Noticias','Datos'].filter(x=>allSignals.some(s=>typeTags(s).includes(x)))];const scopes=['Todos',...new Set(allSignals.flatMap(scopeTags))];chips('typeFilters',types,activeType,v=>activeType=v);chips('scopeFilters',scopes,activeScope,v=>activeScope=v)}
function renderInterests(){const types=['Normativa','Legal','Noticias','Datos'];const scopes=[...new Set(allSignals.flatMap(scopeTags))].filter(Boolean);const make=(kind,vals)=>vals.map(v=>`<label class="interestchip"><input type="checkbox" data-interest="${kind}" value="${esc(v)}" ${interestState[kind].includes(v)?'checked':''}>${esc(v)}</label>`).join('');document.getElementById('interestTypes').innerHTML=make('types',types);document.getElementById('interestScopes').innerHTML=make('scopes',scopes);document.querySelectorAll('[data-interest]').forEach(el=>el.onchange=()=>{const k=el.dataset.interest,v=el.value;if(el.checked&&!interestState[k].includes(v))interestState[k].push(v);if(!el.checked)interestState[k]=interestState[k].filter(x=>x!==v);localStorage.setItem('alicanto_interests',JSON.stringify(interestState));draw()})}
fetch('data/radar_today.json',{cache:'no-store'}).then(r=>r.json()).then(d=>{document.getElementById('meta').textContent=fmtUpdated(d.generated_at);allSignals=d.signals||[];const period=document.getElementById('period');period.value=periodMode;renderFilters();renderInterests();draw();period.onchange=e=>{periodMode=e.target.value;draw()};document.getElementById('sort').onchange=e=>{sortMode=e.target.value;draw()};document.getElementById('interestToggle').onclick=()=>document.getElementById('interests').classList.toggle('open');localStorage.setItem('alicanto_last_visit',new Date().toISOString())}).catch(()=>document.getElementById('local').innerHTML='<div>No se pudo cargar Alicanto.</div>');
